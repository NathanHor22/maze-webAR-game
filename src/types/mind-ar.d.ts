declare module 'mind-ar/src/image-target/three.js' {
  /**
   * MindAR does not publish TypeScript declarations for this entry point.
   * Keep the package export unknown so all runtime-shape assumptions remain
   * isolated in the local adapter.
   */
  export const MindARThree: unknown;
}
